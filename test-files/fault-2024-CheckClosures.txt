Test input file for Hobyah, a 1D tunnel vent program
June 2020
Tests an error message in PROC CheckClosures where
we have a "begin" command that needs a name after it
but does not.

begin settings
end settings

begin tunnel # Dopey
  begin gradients
  end gradients
end tunnel

begin plots
end plots