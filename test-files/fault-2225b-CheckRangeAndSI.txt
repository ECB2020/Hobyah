Test input file for Hobyah, a 1D tunnel vent program
December 2020
Tests an error message where we are expecting a positive number or
zero but get a negative number.  In this version of the error the
number that fails is set as a constant, so there is an extra entry
in the error message.

begin settings
    version 1
    runtype plot
    frictiontype Darcy
end settings

begin constants
  int_neg -1
  int_pos  1
  real_neg -1.
  real_pos  1.
end constants

begin testblock
  0+   0
  0+   2.3
  0+   real_neg
end testblock

begin plots
end plots